#pragma once

namespace EvolutionEngine
{

    class MainCamera
    {
    public:
        void StartDisplayEngine(/* TODO: Fix arguments */);
        bool StartUp(/* TODO: Fix arguments */);
    };

}
